# advoc
Advent of Code

Because we are not allowed to include inputs in a public git repo, the
input data exists as an encrypted zip file created via

```bash
zip -er inputs inputs
```

and which may be decrypted via

```bash
unzip inputs.zip
```
